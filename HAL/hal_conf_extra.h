/*
 * STM32duino HAL module selection.
 * NOTE: the STM32duino core only picks this file up from the sketch/project
 *       include root - keep HAL/ on the include path (see README).
 */
#ifndef HAL_CONF_EXTRA_H
#define HAL_CONF_EXTRA_H

/* Enable STM32 HAL CAN module in Arduino environment */
#define HAL_CAN_MODULE_ENABLED

#endif // HAL_CONF_EXTRA_H
